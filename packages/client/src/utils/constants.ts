export enum EditorType {
    Intellisense = "intellisense",
    Copilot = "copilot",
}

export enum TaskType {
    Authoring = "authoring",
    Modifying = "modifying",
    MultipleChoice = "multipleChoice",
}
